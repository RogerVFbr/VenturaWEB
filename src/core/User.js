export default class User {

    constructor(id, name, userType, documentId, email) {
        this.id = id
        this.name = name
        this.userType = userType
        this.documentId = documentId
        this.email = email
    }
}
